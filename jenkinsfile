pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                echo "Pipeline running..."
            }
        }
    }
    post {
        always {
            echo "Sending email..."
            emailext(
                subject: "Test Email: ${currentBuild.fullDisplayName}",
                body: "This is a test email from Jenkins TASK ",
                to: "amrm97781@gmail.com"
            )
        }
    }
}
